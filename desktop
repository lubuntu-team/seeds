Task-Per-Derivative: 1
Task-Section: user
Task-Description: Lubuntu Desktop
Task-Extended-Description: This task provides the Lubuntu (Ubuntu LXQt environment).
Task-Seeds: desktop-minimal desktop-common
Task-Key: lubuntu-desktop
Task-Name: lubuntu-desktop
Task-Metapackage: lubuntu-desktop

= Lubuntu core packages =

 * (snapd-installation-monitor)

== Internet ==

 * (transmission-qt)

 == Snaps ==

 * (firefox)                        # needed to ensure www-browser is satisfied LP: #2060898
 * (snapd)
 * (packagekit)
 * snap:firefox
 * snap:gtk-common-themes # needed for firefox
 * snap:firmware-updater=1/stable/ubuntu-25.04 [amd64] # Graphical firmware updater tool, see fwupd for CLI tools
 * snap:gnome-42-2204 # needed for firmware-updater, firefox

== Multimedia ==

 * (vlc)
 * (vlc-plugin-access-extra)
 * (vlc-plugin-notify)
 * (vlc-plugin-pipewire)
 * (vlc-plugin-samba)
 * (vlc-plugin-skins2)
 * (vlc-plugin-svg)
 * (vlc-plugin-video-splitter)
 * (vlc-visualization)

== Accessories ==

 * (2048-qt)
 * (featherpad)
 * (noblenote)
 * (screengrab)
 * (qalculate-qt)
 * (qps)
 * (qlipper)
 * (qtpass)
 * (zsync)
 * (partitionmanager)                 # Easy partition management tool
# * (qapt-deb-installer)               # Qt .deb installer tool
 * (kwalletmanager)

== Desktopish tools which we do need ==

 * (usb-creator-kde)                  # Adding utility to make USB bootable
 * (picom)
 * (picom-conf)
 * (redshift-qt)
 * (va-driver-all)                    # Per explicit request from the Desktop Team, LP: #2104767

== Office ==

 * (libreoffice-calc)
 * (libreoffice-impress)
 * (libreoffice-writer)
 * (libreoffice-qt6)
 * (libreoffice-style-breeze)
 * !libreoffice-gnome
 * (qpdfview)
 * (skanlite)

== Desktopish tools that are nice to have ==

 * (fastfetch)                      # nice tool to have by default for distro reviewers, doesn't take up any space
 * (pastebinit)                     # we tell users to use it in the support channels, and it takes up minimal space
 * (htop)                           # superior top replacement that's already in main
 * (inxi)                           # system resource details

== Low level system plumbery ==

# Packages management
 * (plasma-discover)
 * !kde-config-updates              # Possibly dangerous in Lubuntu, causes KDE System Settings to be installed which looks so out of place
